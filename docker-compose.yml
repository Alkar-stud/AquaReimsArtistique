services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: ".env.docker"
    ports:
      - "8000:443"  # Redirige le port 8000 externe vers le port 443 interne (HTTPS)
      #- "8080:80"     # Laissez cette ligne si vous voulez aussi HTTP standard
    #Décommenter les 2 lignes ci-dessous pour mode dev et lier les fichiers locaux de ceux du container
    #volumes:
    #  - .:/var/www/html
    depends_on:
      db:
        condition: service_healthy

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: your_root_password
      MYSQL_DATABASE: aquareimsartistique
      MYSQL_USER: aquareimsartistique
      MYSQL_PASSWORD: M0tDePasseB!donIlFautLeChanger
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3307:3306"
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:5.2
    restart: always
    environment:
      PMA_HOST: db
      MYSQL_ROOT_PASSWORD: your_root_password
    ports:
      - "8081:80"
    depends_on:
      - db

volumes:
  mysql_data: